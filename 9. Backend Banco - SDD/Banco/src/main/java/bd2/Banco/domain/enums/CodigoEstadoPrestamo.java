package bd2.Banco.domain.enums;

/** Códigos conocidos de cat_estado_prestamo (datos semilla). Ciclo: EN_ESTUDIO → APROBADO/RECHAZADO → DESEMBOLSADO. */
public enum CodigoEstadoPrestamo {
    EN_ESTUDIO,
    APROBADO,
    RECHAZADO,
    DESEMBOLSADO
}
