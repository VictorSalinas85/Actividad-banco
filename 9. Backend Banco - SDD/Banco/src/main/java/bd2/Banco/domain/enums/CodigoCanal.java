package bd2.Banco.domain.enums;

/** Códigos de cat_canal_operacion (datos semilla). Requerido por SPs de consignación, retiro y transferencia. */
public enum CodigoCanal {
    VENTANILLA,
    WEB,
    MOVIL,
    BACKOFFICE
}
